{
  "name": "ameq-cenabast",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd backend && npm start",
    "postinstall": "cd frontend && npm install && npm run build && cd ../backend && npm install --omit=dev && mkdir -p public && cp -r ../frontend/build/* public/"
  },
  
  "engines": {
    "node": ">=18.0.0"
  },
  "cacheDirectories": [
    "node_modules",
    "frontend/node_modules",
    "backend/node_modules"
  ]
} 