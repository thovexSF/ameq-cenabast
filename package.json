{
  "name": "ameq-cenabast",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de integración AMEQ-Cenabast",
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd frontend && npm run build",
    "postinstall": "cd frontend && npm install --omit=dev && cd ../backend && npm install --omit=dev"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "concurrently": "^6.2.0"
  }
}
